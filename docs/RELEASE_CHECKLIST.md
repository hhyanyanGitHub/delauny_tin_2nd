# dterrain 1.0 发布验收清单

## 自动门禁

- [x] 核心 Release 构建成功；
- [x] GDAL Release 构建成功；
- [x] 两种构建各 16/16 CTest 通过；
- [x] 31 个关键 DLL C ABI 导出存在；
- [x] 安装后的 `find_package(dterrain 1)` 下游工程编译并运行；
- [x] 100 万随机散点建网、校验、动态编辑和范围查询通过；
- [x] 4096×4096 GRID 流式写入、LOD、DGRIDB 保存/延迟加载/校验通过；
- [x] SDK ZIP 可由 CPack 生成；
- [x] 从实际 ZIP 解压后，独立 `find_package` 消费工程再次编译并运行；
- [x] Markdown 和 Word 手册版本同步。

本次生成物：`dterrain-sdk-1.0.0-Windows-AMD64.zip`（25,369,912 字节），SHA-256：
`DA289C1B2FAAE2C42E6D6CA4D2BDBD7315F45219823C46AFE8B7783AF6FB7497`。

## 本机 v1.0 基线（2026-07-19）

| 场景 | 规模 | 结果 |
|---|---:|---:|
| 普通 TIN 批量建网 | 1,000,000 点 | 3.767 s，约 265,458 点/s |
| 动态插入并删除 | 128 对 | 0.0116 s |
| R-tree 范围查询 | 1,000 次 | 0.0103 s |
| GRID 逐行写入 | 4096² 节点（128 MiB 原始值） | 0.489 s |
| GRID 1024² 平均概览 | 4096² 输入节点 | 0.0231 s |
| DGRIDB 保存 | 4096² 节点 | 0.816 s |
| DGRIDB 延迟映射加载 | 4096² 节点 | 0.0080 s |

数值是当前机器、当前 Release/MinGW 构建的观察结果，不是跨硬件保证。千万点数据应按
数据分布、约束数量、并发任务和 16 GiB 内存重新做项目级容量测试；显示路径应优先使用
DGRIDB、视口 LOD 和瓦片缓存，不应一次复制全量三角面到 GUI。

## 仍需项目所有者决策

- 发布前选择并加入顶层 `LICENSE`；当前仓库没有声明对外开源许可证；
- 核对 CGAL、GDAL、Boost、GMP、PROJ 等实际打包依赖的许可证和再分发要求；
- 如面向生产测绘成果，增加真实项目数据、坐标基准、精度限差和长时间稳定性验收；
- 如发布 GitHub Release，使用生成的 ZIP 并附校验值与本清单。
